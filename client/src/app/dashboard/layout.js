export const metadata = {
  title: "Dashboard",
  description: "Browse the latest public blogs and share your own stories.",
  alternates: {
    canonical: "/dashboard",
  },
};

export default function DashboardLayout({ children }) {
  return <>{children}</>;
}
